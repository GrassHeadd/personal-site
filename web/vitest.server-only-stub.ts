/* Vitest alias target for the `server-only` package, which throws when
   imported outside a React Server environment. Tests run in node and
   import server modules deliberately, so it's stubbed to nothing. */
export {};
